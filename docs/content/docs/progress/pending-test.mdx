---
title: Pending Tests
description: Implemented changes that still need manual verification
---

# Pending Tests

The current release needs manual verification in these areas:

- Deployment Schemes and Pricing Tier Verification:
  - **Tier 1: 极速自助体验包 (¥19.9 一次性)**: Verify price displays as ¥19.9, environment is strictly "仅提供 AutoDL 官方云端预装镜像" (excluding local portable setup), explanation card shows "💡 为什么收 19.9 元？" (cloud image storage cost sharing), disclaimer clearly states "仅限 AutoDL 云端镜像：本方案不包含本地便携包部署" and "遇脚本或网络报错请完全自行排查", click-to-contact modal title & intent note are accurate;
  - **Comparison Matrix & FAQ**: Verify comparison table column displays "仅限 AutoDL 云端镜像（不含本地）" for Tier 1 and "AutoDL 云端 + 本地便携双环境" for Tier 2, and the candid explanation section & FAQ cards correctly reflect the AutoDL-only cloud scope and clear self-service boundaries.

- Canvas Left Sidebar Node List Double-Click Inline Renaming:
  - **Double-Click Title to Rename**: In the left sidebar "Canvas" tab elements list, double-clicking any node's title activates an inline text input with text auto-selected, accompanied by a tooltip "Double-click to rename the node";
  - **Keyboard & Blur Interaction Consistency**: Pressing `Enter` commits the new title, `Escape` cancels and reverts the draft, and blurring outside automatically saves the change; blank inputs safely fall back to the previous title or default untitled fallback;
  - **Conflict Prevention in Multi-Select Mode**: Inline renaming is disabled while in batch selection mode (`selectMode`) to avoid conflicting with row click checkbox selection.

- Canvas Group Node Enclosure, Auto-Capture, and Organization Enhancement:
  - **Selection to Group (⌘G / Ctrl+G) & Exact Coordinates**: When one or more non-group nodes are selected, clicking the "Group" button in the canvas bottom toolbar or pressing `⌘G` / `Ctrl+G` instantly creates a bounding group box around the selection and captures all selected nodes into the group. Fixed the coordinate offset bug caused by factory half-dimension subtraction, ensuring the group frame precisely encloses all selected nodes with padding;
  - **Dynamic Drag & Resize Capture**: When no nodes are selected, a group is created centered at the canvas center. Moving a group over existing stationary nodes or resizing a group's boundaries to encompass nodes automatically captures enclosed nodes into the group upon pointer release; nodes moved outside the group boundaries are automatically released;
  - **Group Toolbar & Context Menu Actions**: Added four dedicated quick actions on the group hover toolbar and right-click context menu: "Select All in Group", "Capture Enclosed Nodes", "Fit to Content", and "Ungroup (`⇧⌘G` / `Ctrl+Shift+G`)";
  - **Double-Click Group to Select Children**: Double-clicking a group node directly selects all of its enclosed member nodes for rapid batch manipulation; ungrouping cleans up the virtual container while preserving all member nodes in place.

- Video Preview Modal and Canvas Playback Stability Fix:
  - **Retain Source Attribute Across Renders**: Removed DOM `removeAttribute("src")` cleanup which was stripping the video source during React StrictMode mount/cleanup cycles;
  - **Safe Autoplay with Browser Policy Fallback**: Gracefully catches browser autoplay policy restrictions in `CanvasPreviewVideo` so controls remain fully operational without video load crashes;
  - **Gesture Continuity in Canvas Play Button**: Changed center play button from conditional unmounting to CSS visibility transition so double-click gestures maintain DOM continuity to reliably open preview modals;
  - **Explicit muted Property Synchronization**: Added DOM-level synchronization for `video.muted = isMuted` to guarantee compliance with browser autoplay restrictions.

- Default Canvas Image Generation Count Adjustment:
  - **Default Value Updated to 1**: Adjusted `defaultConfig.canvasImageCount` in `useConfigStore` and its rehydration fallback from `3` to `1`, aligning with the single-image baseline for newly created canvas image and configuration nodes.

- Canvas Video Node Playback Interaction and Control Optimization:
  - **Decouple Node Drag/Select from Video Playback**: Replaced native `<video controls>` with a dedicated `CanvasVideoPlayer` component, preventing unintentional playback when clicking or dragging video nodes across the canvas;
  - **Lightweight Floating Controls**: Added a semi-transparent center play button (when paused) and a floating bottom control bar with scrubbable progress range slider, play/pause toggle, timestamp display (`00:03 / 00:05`), mute/unmute toggle (muted by default on canvas with loop enabled), and modal preview trigger;
  - **Smooth Controls Auto-hide**: When playing, the floating control bar smoothly fades in upon mouse hover and automatically fades out when the mouse leaves;
  - **Double-Click & Toolbar Modal Preview**: Double-clicking a video node or clicking the new "View video" (Eye icon) button in the hover toolbar opens a high-definition media preview modal with native controls, looping, and full audio;
  - **Frame Capture Compatibility**: Preserved the `data-canvas-video` contract and synchronized `currentTime`, allowing accurate frame scrubbing and right-click context menu extraction of the "current frame".

- Text Node LLM Generation Completion Stop Status Fix:
  - **Immediate Deregistration on Completion**: Fixed an omission in the text generation callback where `finishGenerationRequest` was not called for `rootId`, leaving the new text node in `runningNodeIds`; now deregistrations run immediately upon LLM job completion and abort handling;
  - **Global finally Cleanup Fallback**: Added iteration over `pendingChildIds` in `handleGenerateNode`'s `finally` block to deregister any child request IDs that were not already cleaned up, eliminating the persistent spinning "Stop" button after generated text has successfully rendered on screen.

- Text Node and Canvas Node "Generate Again" (Repeat) Connection Logic Unification:
  - **Prohibit Connecting to Current Node**: Fixed an issue where repeating generation would mistakenly fall back to connecting from the current node if the current node had no upstream parent connections (e.g. standalone text nodes); "Generate again" now strictly adheres to "only inherit upstream parent connections" — if no parent exists, the new node has no incoming connections, preventing erroneous serial chaining;
  - **Consistent Cross-Modality Sibling Topology**: Text nodes now share identical connection semantics with Image, Video, and Audio nodes when repeating, always generating parallel sibling nodes.

- Batch Image Generation Stop Cancellation and Loading Spinner Fix:
  - **Slot Status Evaluation Fix**: Fixed a bug in `<CanvasNode>`'s `ImageSlotStatus` where any non-error state was erroneously treated as loading and rendered with an infinite spinner. Added an explicit check for `loading`; cancelled/idle slots now correctly display the empty slot icon and "No image yet" text;
  - **Batch Stop State Settlement**: When clicking "Stop", `stopGenerationByRunningId` and `handleGenerate` abort handling now cleanly abort the ComfyUI background job, retain any already generated images as primary/successful output, reset uncompleted loading slots to idle, and settle the root node status to success or idle. This prevents image cards from getting permanently stuck in the generating state after cancellation.

- Image, Video, Text, and Audio Node "Generate Again" (Repeat) Successful Output On-Screen Display and Metadata Isolation Fix:
  - **Fix Undefined Variable in Text Generation**: Added the missing declaration for `sourceTextContent`, fixing the `sourceTextContent is not defined` runtime error encountered during text generation;
  - **Prune Cloned Stale Primary IDs and Sub-items**: When clicking "Generate again" on Image, Video, Text, or Audio nodes, `primaryImageId`, `primaryTextId`, `images`, `texts`, and obsolete media content/dimension fields (`naturalWidth`, `naturalHeight`, `bytes`, `mimeType`) are cleanly reset to prevent callbacks from mistakenly treating the node as having an existing completed item;
  - **Active Content Safety Check**: In the single/batch image completion callback, only return early without overriding primary properties if a primary image with valid `content` already exists, ensuring the newly generated image correctly sets `content`, `storageKey`, `primaryImageId`, and responsive dimensions;
  - **Multi-tiered Rendering Fallback**: Added fallbacks in the job completion callback and `<CanvasNode>` rendering layer (across image and text renderers) to resolve and render active content from items whenever content exists, eliminating the perpetual loading spinner.

- Canvas Node Copy / Duplication (Context Menu "Duplicate", Ctrl+C / Ctrl+V Paste, Batch Image "Create Copy") Connection Preservation:
  - **Single Node Duplicate Connection Inheritance**: Right-clicking any node (Image, Text, Video, Audio, Config) and choosing "Duplicate" creates a new node (with ` Copy` suffix) that inherits all configuration/parameters AND automatically replicates all incoming parent connections via `duplicateIncomingConnections`, ensuring identical upstream input context;
  - **Batch Image Card "Create Copy" Connection Inheritance**: Clicking "Create copy" on an expanded batch item to extract a single image node now replicates all parent connections from the parent batch node;
  - **Clipboard Copy / Paste (Ctrl+C / Ctrl+V) Connection Preservation**: Copying and pasting single or multiple nodes using `buildPastedNodeConnections` maintains both internal connections between copied nodes (e.g. A->B maps to A'->B') and external connections from existing canvas parents (e.g. Parent->A maps to Parent->A'), avoiding orphaned isolated nodes;
  - **Connection Deduplication and Safety Validation**: Validates that upstream parents exist on the canvas, filters out references to deleted nodes, and deduplicates identical connection edges from the same parent.

- Text, Video, and Image "Generate Again" (Repeat) Aligned as "Copy and Regenerate with New Seed":
  - **Node Property & Metadata Inheritance**: When repeating generation, the new node fully inherits the original node's title, aspect ratio / dimensions (preserving custom dimensions without collapsing back to 512 default size), and all workflow parameters (model, prompt, size ratio, quality, video duration/mode, etc.). It aligns horizontally with the original node, only clearing transient execution states and injecting a newly generated random seed;
  - **Connected Directly to Parent Nodes**: Fixed an issue where repeating generation erroneously connected the new node as a child of the current node (forming a serial chain). Using `buildGeneratedNodeConnections`, the system automatically replicates the upstream parent connections of the current node (lineage and input references) to the new node, ensuring repeated generations form a parallel sibling topology. If the current node has no parents, it gracefully falls back to connecting from the current node;
  - **Based on Current Connected Lines**: "Generate again" no longer relies on frozen historical snapshots (`restoreGenerationContext`). Instead, it dynamically queries the incoming wires currently connected to the node on the canvas (`buildNodeGenerationContext`), immediately reflecting any wire adjustments or new reference nodes;
  - **New Seed on Re-submission**: Clicking "Generate again" or pressing Enter generates a fresh random seed distinct from the previous run via `generateNextSeed`, explicitly forwarded across image (`requestGeneration` / `requestEdit`), video (`requestVideoGeneration`), and text (`requestImageQuestion`) pipelines;
  - **Complete Removal of Derive**: Cleaned up the "Generate from result" button and its `derive` dispatch branches across all modalities (image, video, text, audio) to eliminate implicit injection of current outputs into reference slots that caused unexpected workflow mode drift;
  - **Type and Locale Cleanup**: Removed `"derive"` from `CanvasGenerationIntent` and pruned deprecated locale keys (`deriveGeneration`, `frameVideoDeriveUnsupported`, `editTextPrompt`).

- Settings & Preferences Full Persistence Across Page Refresh:
  - **Storage Migration to LocalForage**: Migrated `useConfigStore` and `usePromptSourceStore` to `localForageStorage` (with backward-compatible localStorage migration fallback) to avoid browser 5MB quota exhaustion;
  - **Channels Persistence**: Adding, editing (proxy URL, token, name, 6 modal workflow slots), deleting (cloud channel deletion honored), and reordering (setting default channel) persist across page refresh;
  - **Preferences Persistence**: Default models (`imageModel`, `videoModel`, `textModel`, `audioModel`), canvas image count (`canvasImageCount`), TTS settings (`audioVoice`, `audioFormat`, `audioSpeed`, `audioInstructions`), and `systemPrompt` all persist across page refresh;
  - **Prompt Sources Persistence**: Built-in source enable/disable toggles, custom added prompt sources, and scheduling configuration persist across page refresh;
  - **WebDAV Persistence**: WebDAV URL, directory, username, password, and last sync timestamp persist across page refresh.

- Native Built-in Dual Channels (Local ComfyUI & Cloud ComfyUI):
  - **Dual Channels Provisioning**: On fresh startup or configuration reset, the system provides both "Local ComfyUI" (`http://127.0.0.1:8188`) and "Cloud ComfyUI" (user-configured cloud endpoint) as built-in channels, encompassing 12 pre-provisioned models (`local::...` and `cloud::...`);
  - **Differentiated Workflows & Weights**: The local channel binds workflows targeting consumer GPUs with pruned/FP8 weights; the cloud channel binds workflows targeting high-VRAM cloud GPUs with full BF16 weights and 8-step Turbo acceleration;
  - **Context-Aware Default Restoration**: In the channel editor drawer, "Restore Default Workflow" restores the local workflow for Local ComfyUI and the cloud workflow for Cloud ComfyUI;
  - **Precise Request Routing**: Executing generations with models bound to local or cloud channels correctly routes requests to the corresponding backend instances.

- Multi-Channel Support in Guide Page "Service Probe":
  - **Title Update**: Updated "Local Service Probe" to "Service Probe" to encompass both local and remote/cloud ComfyUI channels;
  - **Multi-channel Selection**: Provides a multi-select dropdown for choosing configured ComfyUI channels, with quick "Select All" and "Clear" actions;
  - **Concurrent Parallel Probing**: Clicking "Probe Selected Channels" concurrently pings each selected channel's `/system_stats` endpoint and reports aggregate status;
  - **Itemized Channel Cards**: Details channel name, URL endpoint, default channel tag, online/offline status, latency in milliseconds, or connection error messages;
  - **Single Channel Quick Re-test**: Includes a dedicated refresh button on each channel row to re-test individual channels without triggering a full re-probe.

- Full Integration of 5 Mandatory ComfyUI Custom Node Plugins in User Guide & Documentation:
  - **Guide Page Section**: Visiting `/guide` displays a dedicated "5 Mandatory ComfyUI Custom Node Plugins" section beneath the 4-step quickstart, listing the 5 plugins (Comfyui-kktools, ComfyUI-KJNodes, ComfyLiterals, ComfyUI-UniversalToolkit, ComfyUI_LayerStyle) with names, repository links, descriptions, and applicable workflow tags;
  - **One-Click Script Copying**: Includes an "Copy All Git Clone Commands" action button as well as per-plugin clone command copy buttons;
  - **Workflow Card Badges**: The 6 core workflow cards clearly display plugin badges with external GitHub links for Inpaint, Text Generation, FL2V, and Omni Video;
  - **All-in-One ZIP Package**: Downloading the workflow zip includes an updated `README_模型与存放目录清单.txt` with plugin repositories and clone commands;
  - **Documentation Sync**: `docs/COMFYUI_WORKFLOW_GUIDE.md` (both Chinese & English) Section 2 details prerequisite plugin tables and installation instructions;
  - **Standalone Consolidated Guide**: `docs/COMFYUI_PLUGINS.md` consolidates all 5 mandatory plugins with summary table, one-click clone scripts, workflow matrix, deep node breakdown, and troubleshooting FAQ, indexed in `docs/index.md`.

- Preserve Source Image as Reference on Mask Edit Derived Node:
  - **Automatic Input Connection**: After completing brush painting and "AI Edit" in "Local Mask Edit" on an image node, the newly generated node automatically establishes an `input` reference connection with the source image;
  - **Reference Bar Display**: Expanding the bottom prompt bar on the new node displays the previous repainted base image thumbnail in the "References" section, supporting hover preview and disconnection;
  - **Downstream Generation Inheritance**: Future prompt submissions or generation requests on the new node inherit the source base image for image-to-image workflows.

- Derived Target Node Generating State & Submission Lock:
  - **Target Node Running Perception**: Any new node derived from another node ("Generate again" or "Generate from result") is strictly treated as "generating" during its active generation process on both the card and the bottom prompt bar;
  - **Prompt Bar & Stop Interaction**: If the new node is selected to view the bottom prompt bar while generating, the action button renders as a spinning "Stop" button and strictly disallows clicking the upward arrow to duplicate submissions; clicking "Stop" cleanly cancels the task and resets the node;
  - **Ready for Submission upon Completion**: Once generation succeeds, the new node transitions to ready state, unlocking "Generate again" and "Generate from result" as valid submittable actions.

- Canvas Node Prompt Panel Bottom Toolbar Collision Prevention:
  - **Component Layout & Separation**: Left controls (expand, prompt library, model picker, popover settings) and right action buttons ("Generate" or "Generate again" + "Generate from result") display side-by-side without overflowing or overlapping each other.
  - **Width & Flex Wrapping**: Upgraded panel width to 720px, added `min-w-0` truncation protection, and enabled `flex-wrap` fallback with `ml-auto` alignment for ultra-tight contexts.

- Remove "Quality" and "Transparent Background" from Image Workbench & Canvas Image Generation Nodes:
  - **Image Workbench**: The image settings panel removes the "Quality" pill options and "Transparent Background" switch; mobile bar summary text no longer displays the quality field.
  - **Canvas Image Generation Nodes**: The image settings popover for canvas prompt toolbar and image config nodes removes "Quality" and "Transparent Background", retaining only dimensions, aspect ratio, and count controls.

- Multi-channel ComfyUI (Different IPs) Independent Requests & Exact Routing:
  - **Channel Prefix Preservation**: Preserves the full `channelId::` model identifier during image, video, text, and inpaint generation, preventing fallback to the first channel caused by premature stripping via `modelOptionName`.
  - **Decouple Node Model from Global Preference**: Prioritizes the node's own model (`req.config.model`) over global preferences (such as `config.videoModel` or `config.textModel`), ensuring that node-specific channel selections are respected.
  - **Preserve Current Channel on Video Mode Toggle**: Toggling between "Omni" and "Frame" on a video node stays on the node's currently selected channel instead of hardcoding `channels[0]`.
  - **One-click Set as Default Channel**: In the "Config - Channels" tab, the first channel displays a "Default" tag while other channels have a "Set as default" button that reorders the channel to first and syncs default models.
  - **Custom Workflow Priority & Safe Fallback**: Prioritizes `channelModel.comfyuiWorkflow` over channel-level defaults across all modalities (T2I, I2I, inpaint, text, and video), ensuring user-uploaded custom workflows on specific models are strictly executed. Removed cross-capability `models[0]` fallback.

- Intent-driven generation and connection semantics: verify that existing `input` connections remain intact, generated results add only `lineage` connections, and `repeat`, `derive`, `retry`, and `resume` each use the intended request inputs.
- Concurrent jobs & cancellation: cancel one job while others continue independently; after `GET /queue`, confirm a queued target is deleted with `POST /queue`, while `/interrupt` is used only when that exact target is running.
- Fail-loud validation: invalid model inputs, excessive Frame inputs (>2 reference images or audio/video inputs), and an empty generation result, should each surface an explicit localized error instead of failing silently or leaving the node in a broken state. Frame video mode now supports 0-2 reference images (supports text-only generation without reference images).
- Stable `@[node:id]` references: `@[node:id]` tokens should stay stable across edits, refresh, and re-referencing without duplicating or reordering the underlying connection.
- Agent invalid connections: the canvas Agent should reject missing-node, self, Config-source, Group-target, and duplicate connections, leaving no invalid edge in the snapshot.
- Nested groups: groups nested inside groups should collapse, expand, reference, and generate correctly, including when the inner and outer group are referenced by the same generation node.
- Source media status: when a generation derived from a successful image/video/audio/text node fails, the source node should keep its original content and `success` status.
- Component Job ID Binding & Resume Polling:
  - Immediately binds `jobId` to the target component upon submission across all modalities (single image, batch images, derived edits, inpaint masks, videos, and texts).
  - On page reload, unexpected interruption, or timeout, components retain the `jobId` and display the "Query again" (重新查询) action instead of degrading to resubmission.
  - Clicking "Query again" invokes ComfyUI `/history/{jobId}` directly to fetch the finished asset without restarting GPU computation.

- User Manual & ComfyUI Workflow Downloads Page (/guide):
  - **Comprehensive 6-Workflow Coverage**: Built-in cards and JSON downloads for all 6 core workflows (Z-Image-Turbo T2I, Flux2.Dev I2I, Qwen Inpaint, Qwen 3.5 LLM, MiniMax H3 FL2V, and MiniMax H3 Ref2V).
  - **One-click ZIP Packager**: Bundles all 6 workflow JSONs and a structured `README_模型与存放目录清单.txt` with fflate for client-side offline saving.
  - **Local Model Directory Checklist**: Exact folder paths (`models/diffusion_models/`, `models/vae/`, `models/text_encoders/`, `models/loras/`) with one-click copy buttons for both individual filenames and entire path checklists.
  - **ComfyUI Connectivity Probe & Launch Command**: Real-time diagnostic ping to the user's configured ComfyUI IP & port (from config store, falling back to default 8188) with `--enable-cors-header "*"` guidance and one-click copyable typical launch command (`python.exe main.py --listen 0.0.0.0 --enable-manager --enable-cors-header "*"`).
  - **Global Seamless Navigation**: Dedicated links from top navigation bar (replacing external doc link), mobile navigation drawer, home page hero section, and config page header banner.

- Fix Configuration & User Preferences Refresh Mutation Bug:
  - **Symmetric Workflows & Default Fallbacks**: Added `comfyuiT2iWorkflow` to `ModelChannel` and passed `defaultWorkflow={DEFAULT_COMFYUI_*_WORKFLOW}` to all 6 workflow editors, preventing workflow status flips (Built-in / Custom / Unassigned) and revert issues on browser reload.
  - **Safe Model Preference Persistence**: Added capability-aware fallback during `merge` for `imageModel`, `videoModel`, and `textModel`, ensuring that obsolete or unselected values never wipe settings to empty strings.
  - **Atomic Save & Storage Version Migration**: Introduced `setConfig` for atomic single-pass persistence to `localStorage` and bumped storage `version: 1` to cleanly isolate legacy data.

- ComfyUI Video Output Download 404 Fix:
  - **Retain Subfolder Parameter**: Preserves full query parameters (`/api/view?filename=xxx.mp4&type=output&subfolder=video`) when ComfyUI returns a `subfolder` field (such as `video`), preventing 404 errors caused by dropping `subfolder`.
  - **Video Extension Detection**: Automatically classifies video files (`.mp4`, `.webm`) even when returned inside the `images` output list by custom nodes.
  - **Dual Endpoint Fallback**: Supports both `/api/view` and `/view` endpoints with automatic fallback on 404.

- System Built-in ComfyUI Workflows & Custom Binding Management:
  - **Built-in Default Bindings**: The 6 standard API workflows from `Comfy/Comfy-Api` (`comfyuiT2iWorkflow_api.json`, `comfyuiI2iWorkflow_api.json`, `comfyuiInpaintWorkflow_api.json`, `comfyuiTextWorkflow_api.json`, `comfyuiVideoWorkflow_api.json`, `comfyuiFrameVideoWorkflow_api.json`) are built directly into the system for out-of-the-box operation.
  - **Custom Workflow Indicator & Reset**: Workflow cards in the channel editor drawer show clear "Built-in" (系统内置) or "Custom" (自定义) tags with file size. When a custom workflow is uploaded, users can click "Reset to Default" (恢复默认) to return to the built-in workflow, or clear it.
  - **Execution Fallback**: Generation for T2I, I2I, Inpaint, Text inversion, and Video automatically falls back to system defaults if no custom workflow is uploaded.

- Video Node "Omni Reference" & "Frame Video" Strict Isolation & Two-way Sync:
  - **Bi-directional State Sync**: Toggling between "Omni (全能参考)" and "Frame (首尾帧)" in the video settings popover automatically updates the ModelPicker to `ComfyUI Video` or `ComfyUI Frame Video` respectively, and vice versa.
  - **Strict Workflow Routing**: "Omni" mode strictly invokes `comfyuiVideoWorkflow`, while "Frame" mode strictly invokes `comfyuiFrameVideoWorkflow` without cross-fallback confusion.
- Full Modal Nodes (Text, Image, Inpaint, Video) Support "Query Again":
  - **Real-time jobId Persistence**: Once submitted to ComfyUI `/prompt`, the returned `jobId` is immediately recorded into the node metadata and saved locally.
  - **Interruption Recovery**: Whether reset on page reload or timed out, any node with an existing `jobId` shows "Query again" (重新查询) on both card and toolbar.
  - **Zero Waste Polling**: Clicking "Query again" directly queries `/history/{jobId}` without resubmission or duplicating GPU computation. If no `jobId` exists, it stays "Retry" (重新生成).
- Channel Editor Drawer Simplification: Removed the redundant bottom "Provider Models" list module. All capabilities and workflows are now directly managed and uploaded via the dedicated workflow cards (T2I, I2I, Inpaint, LLM, Video, Frame Video), making the interface clean and straightforward.
- Direct Connection to Native ComfyUI 8188 API:
  - **Default URL & Auth**: Channel management default URL is updated to `http://127.0.0.1:8188`. Token is now optional to seamlessly support standard local unauthenticated ComfyUI instances.
  - **Native Endpoints**:
    - Asset Uploads: Directly calls `POST /upload/image` with FormData into the ComfyUI `input` folder, binding plain file names to node inputs.
    - Workflow Submission: Directly calls `POST /prompt` with `{ prompt, client_id }`, handling `node_errors` validations immediately.
    - Job Polling & Outputs: Directly polls `GET /history/{prompt_id}` and extracts `images`, `videos`, `gifs`, `audio`, and `text` output channels.
    - Asset Viewing & Downloads: Directly calls `GET /view?filename=xxx&type=output` to render images/videos.
    - Cancellation: Reads active jobs via `GET /queue`, deletes a queued target with `POST /queue`, and calls `POST /interrupt` only when that exact target is running.
- Video Node & Workflow Omni Reference vs First/Last Frame Dual Modes:
  - **Channel Workflows**: Channel configuration independently separates "Video Generation Workflow (Video / Omni)" and "First & Last Frame Video Workflow (Frame Video)", with default models `ComfyUI Video` and `ComfyUI Frame Video`.
  - **Mode Switch**: Video settings panel and canvas popover button support instant toggling between "Omni Reference (Omni)" and "First / Last Frame (Frame)".
  - **Omni Reference Mode**: Supports up to 9 images, 3 videos, and 3 audios with auto routing to `ref_image_01..09` / `ref_video_01..03` / `ref_audio_01..03` and automatic unassigned branch pruning.
  - **First & Last Frame Mode**: The first connected image is assigned to `first_frame`, and the second connected image to `last_frame`. If only 1 image is connected, the unassigned `last_frame` node is cleanly pruned, enabling single-image start frame video generation or dual-image start-to-end interpolation video generation.
- Separate T2I and I2I Workflow Management: In Channel Management outer card and drawer, "Text-to-Image Workflow (T2I)" and "Image-to-Image Workflow (I2I)" are presented as independent upload cards, with default models `ComfyUI T2I` and `ComfyUI I2I`. When generating purely from text prompts on canvas, the system invokes the T2I workflow; when connecting reference images (single-image edit, style transfer, dual-image fusion), it automatically routes to the I2I workflow.
- Flux2 Image Editing & Dual-Image Fusion Pipeline: Supports image editing and multi-image blending (up to 2 reference images) based on the Flux2 workflow (`image_flux2_fp8_api.json`). When connecting 2 images on the canvas, they are automatically uploaded and mapped to `ref_image_01` and `ref_image_02` slots. When connecting 1 image, unassigned `ref_image_02` branches and intermediate scaler/encoder nodes are pruned while `ReferenceLatent` cascades are dynamically bridged to the first reference latent. When generating with 0 images, all ReferenceLatent nodes are cleanly bypassed to FluxGuidance with scheduler dimensions rewired for pure text-to-image.
- Video generation resume polling on timeout: When a video job is successfully submitted to ComfyUI, the system records and persists its `jobId` in node metadata. If a timeout occurs due to slow local GPU rendering or queueing, the node enters an error state with its `jobId` preserved, and the action button on both card and hover toolbar turns into "Check status again". Clicking it resumes polling the existing `jobId` without re-submitting or wasting GPU cycles.
- Video duration 5-15s slider & quick presets: In video settings panel and popover, duration can now be adjusted smoothly via a 5s to 15s continuous slider with tooltip formatting, alongside three quick-select pill buttons for 5s, 10s, and 15s. The title dynamically reflects the current second value with bi-directional synchronization, injecting precisely into workflow duration calculation nodes.
- ComfyUI 2-hour job timeout window: The global polling timeout for asynchronous ComfyUI jobs has been raised from 10 minutes to 2 hours (7,200s), ensuring heavy video diffusion models, long-step samplers, and queued execution on local GPUs are not prematurely aborted; error messages now format duration cleanly (e.g. `after 2h`).
- Multimodal reference video generation pipeline: Supports connecting up to 9 image nodes, 3 video nodes, and 3 audio nodes to generation config or video nodes on canvas. The system automatically uploads references via the native ComfyUI upload endpoint and routes them in FIFO connection order to `ref_image_01..09`, `ref_video_01..03`, and `ref_audio_01..03` slots. Unassigned slots and their intermediate bridge nodes (e.g. `GetVideoComponents`) are pruned completely from the workflow JSON with incoming references removed from the main node to ensure smooth text-to-video or arbitrary reference count execution. Fails loud with explicit errors if user connects inputs exceeding slot limits.
- MiniMax video size presets: The video generation settings panel replaces free-form W/H inputs with strict 16:9 landscape and 9:16 portrait ratios, locking resolution into 9 discrete presets from 0.2M to 0.98M (up to 1344x768 / 768x1344). Preset cards directly display exact pixel width and height with secondary tier tags, dynamically swapping dimensions when toggling aspect ratios and injecting into workflow nodes.
- ComfyUI video generation & unified workflow group: In Channel Management outer card and edit drawer, all ComfyUI workflows (Text-to-Image T2I, Inpaint, LLM Text/Reverse, and Video) are unified in the same workflow group with dedicated upload and clear controls. Added ComfyUI Video as a pre-provisioned model and default video model, and video generation in canvas/workbench now executes via ComfyUI.
- ComfyUI LLM text generation & visual reverse prompt: The channel settings include a default ComfyUI LLM model and a dedicated "Text Generation / Reverse Prompt Workflow (LLM)" upload card, bundled with the Qwen 3.5 VLM workflow. Clicking "Reverse prompt" on an image node and generating should upload the image via the native ComfyUI upload endpoint into the `image` node, inject instructions into the `prompt` node, and output the reversed prompt to a new text node. When generating without an image, it automatically strips the `_meta.title=image` node and its input links for text-to-text execution.
- ComfyUI inpaint workflow: In Channel Management for ComfyUI, a dedicated "Inpaint Workflow" upload entry should be visible, allowing users to upload and persist the inpaint workflow JSON. Default model list includes ComfyUI Inpaint. When painting a mask on an image and clicking "Edit with AI", the base image and mask should automatically upload via the native ComfyUI upload endpoint, inject into `ref_image` and `ref_mask` slots, submit, and download the edited image.
- Direct raw text replacement in prompt assembly: When referencing a text node via @ in a config node or prompt input, the submitted prompt should directly replace the reference with the text node's raw content, without adding [Text 1] tags, numbering, or append blocks.
- ComfyUI-only channels: The channel system has been simplified to exclusively support local ComfyUI channels, removing OpenAI, Gemini, and other external LLM API endpoints. The channel editor displays only the ComfyUI URL and optional access token, and defaults to ComfyUI T2I. All canvas interactions, asset uploads, and node operations are preserved.
- Canvas Agent generation-flow references: when a generation prompt contains only `@[node:...]` references already passed through `referenceNodeIds`, the existing text, image, video, audio, or group nodes should connect directly to the generated config node without creating a duplicate prompt text node; prompts containing ordinary prose should continue creating their own prompt text node.
- Video frame capture: right-clicking a video node with content should show Capture first frame, Capture last frame, and Capture current frame. Each action should create and select a proportional image node to the right, connect the video to the image, use the player's visible position for the current frame, and leave the original playback position unchanged.
- Text nodes: the Generate button should no longer appear in the upper-right corner of the node body, and text should use the full content area. The image-generation action in the hover toolbar should continue to create a generation configuration.
- Multi-text generation: requesting more than one result should create one text-result node and one source connection. Its upper-right count should expand all successful alternatives like a multi-image node; setting an alternative as primary should immediately replace the body while retaining the previous primary, and edits to any primary text should remain when switching away and back.
- Group references: group nodes should use a transparent, lightweight border and title treatment and expose a source handle on the right. Connecting a group to a generation node should include every populated image, video, audio, text, and reference-capable extension node inside it. Regular prompt mentions should list the group's resources, while the generation-config composer should allow selecting the whole group with one `@`; resources referenced both individually and through the group must be sent only once.
- Node reference bar: regular prompt panels, expanded editors, and generation-config inputs should show their current input nodes above the editor. Images and videos should use thumbnails with hover previews, while text, audio, and extension nodes show their matching type. A group connection should expand into every valid content node inside the group, and removing any expanded item should disconnect the whole group. The plus button should enter canvas selection for adding multiple valid references. Connected nodes, the target itself, children already referenced through a group, and unsupported nodes must not be selectable again; ESC or the top hint should return to the input, and new connections should immediately update the `@` list.
- Multi-image transparency: when a multi-image node containing transparent PNG or WebP assets is expanded, transparent areas in both the primary and alternatives should reveal the canvas; switching primary images must not introduce a white or panel-colored background.
- Canvas navigation: the canvas should open in Move mode by default, and the bottom toolbar should switch between Select and Move modes and reflect the active mode with its icon; Select should draw a clearly spaced dashed range whose dash length, gap, and stroke width remain visually constant at every zoom level, while Move should drag elements directly and pan only when dragging the blank background. Clicking blank canvas space in either mode should deselect the current node and close its top toolbar and lower prompt panel. Holding Control or Space should temporarily invert the active tool without changing the toolbar mode: Select becomes Move, and Move becomes range selection. After clicking any canvas-page button, pressing Space should only invert the tool and must not trigger that button again. Middle-button panning from any position and additive node selection with Shift/Cmd should continue to work, while Space remains available inside text inputs and editable content.
- English and Simplified Chinese switching across navigation, settings, titles, descriptions, Ant Design components, and persisted preferences.
- Branding & credit: the home hero title should read "ComfyUI Infinite Canvas"; the top nav should no longer show the version-updates button or the GitHub icon, replaced by a "Special thanks" link crediting the upstream open-source project basketikun/infinite-canvas (clickable, correct text in both languages).
- Global Dropdown, Menu, Select, Cascader, and TreeSelect popup backgrounds, hover states, and selected states in both light and dark themes.
- Canvas node resize stability: importing an image should open its prompt panel, resizing it immediately from any corner should temporarily hide the panel, remain smooth without flashing or triggering a maximum update depth error, and restore the panel after release. Prompt scrolling, generated-prompt restoration, edit-prompt persistence after closing an existing image or text node panel, expanded prompt editing with live synchronization and `@` references, image editing, drag-and-drop references, and generation configuration should continue to work.
- Text output count: both a text node's lower prompt panel and a generation configuration node in Text mode should expose an independent generation count from 1 to 15, default to one, and create that many text nodes. Changing the text count must not alter the image count, and switching a generation configuration node back to Image should continue to use the configured image count.
- Node editing entry points: the image toolbar should default to icons without labels and no longer include Edit; the text toolbar should no longer include Edit or Edit text. Clicking an image, text, or video node without dragging should open its lower prompt panel directly, while image toolbar labels can still be enabled explicitly from the toolbar settings.
- Canvas side panel: clicking an element row should smoothly center and select its node without zooming above 100%; image elements with content should still open the large preview without triggering canvas focus.
- 画布左侧树形列表：组内节点应缩进显示在所属组下方，并带有清晰的层级连接线；组箭头可以收起和展开子节点，搜索或类型筛选时仍应保留匹配节点所属的组。
- Multi-image canvas generation: starting N images should immediately create one image node with N expandable slots and one incoming connection; each empty slot should independently show generating or failure while requests are running, every slot with image content should always display that image, and the first successful image should appear as the primary image immediately. The collapsed state should use a compact stack with at most three closely spaced backplates and a clearly legible count control; only clicking the count control should expand or collapse the slots upward and to the right, while double-clicking the image should open its large preview. Every completed secondary image should keep a clearly legible primary-image action visible in both themes. Expanded groups should remain expanded when clicking elsewhere, dragging, resizing, or previewing, and multiple groups may remain expanded together.
- Multi-image primary selection: choosing another primary image should preserve the node center and current maximum edge while adapting to the new image ratio; free-resize nodes should keep their exact dimensions, and the viewport zoom indicator must not change.
- Failed multi-image slots: every failed slot, including the current primary slot, should expose retry and delete actions. Retry should update only that slot without replacing an existing successful primary image; delete should remove only that failed slot and update the displayed count.
- Multi-image resize: resizing the root node while child images are expanded should keep the group expanded and resize normally without triggering nested React updates or an application error.
- Multi-image alternatives: expanded images should use up to four columns so larger batches stay compact; double-clicking any completed child image should preview that exact image without changing the primary image. Every completed image, including the primary image, should provide an independent download action, while alternatives should also keep clearly legible create-copy and set-primary actions in both themes. Create-copy should add an independent image node to the right of the group, select it, and leave the original image group unchanged.
- Canvas Agent startup, MCP status, model and permission controls, approvals, version reporting, diagnostics, streaming responses, history consistency, multi-tab isolation, and local Skill management.
- Canvas Agent credential redaction: Codex and Claude CLI stderr containing Bearer tokens, `sk-` keys, explicit API key/token/authorization assignments, ANSI formatting, or credentials split across output chunks should reach neither debug logs nor browser diagnostic events in plaintext; ordinary stderr text should remain intact.
- Image API model parameters: `gpt-image-*` generation and edit requests should omit `response_format`, while `dall-e-*` requests should continue sending `response_format: "b64_json"`.
- Image URL persistence: when an image API returns a remote URL, the client should immediately download it into self-contained image data before normal local persistence; if browser CORS blocks the download, the original URL should remain available as a fallback.
- Agent header tabs in English and Chinese at different panel widths; labels should collapse to icons and counts before either edge is clipped.
- Agent message layout, Markdown, code blocks, attachments, token statistics, progress timelines, command groups, and scroll-follow behavior.
- Agent inline references: type `/` to select an enabled Skill or `@` to select a canvas asset at the caret; tokens should remain aligned with text, support keyboard navigation and hover previews, and preserve their positions after sending or refreshing.
- Agent message metadata: image attachments, Skills, canvas references, and local previews should survive page refreshes and Agent restarts through exact `clientMessageId`, `threadId`, and `turnId` ownership; protocol upgrades must not reset the independent storage format, unknown or damaged storage must refuse writes without overwriting files, no records may be silently pruned by count or size, and deleting a thread should remove its metadata and preview assets without relying on browser IndexedDB or text matching.
- Prompt Center layout, source synchronization, search, asset insertion, prompt detail dialogs, and compact split-layout prompt pickers in the image and video workbenches.
- Workbench history image cleanup: after generating content in the image or video workbench, deleting an asset, canvas image node, canvas content, or canvas project must not remove thumbnails, results, or references from generation history; deleting a generation record should still remove its dedicated local files.
- Settings import/export and WebDAV-related configuration.
- Canvas node duplication (right-click "Duplicate" and keyboard copy-paste) integrity and semantics: unifying deep-cloning with `cloneNodeMetadata` across both right-click and Ctrl+C/V paths; resetting transient generation state (`status`, `jobId`, `errorDetails`, `isTimeout`) to ungenerated ready state with the "AI 生成" button; regenerating unique IDs and clearing `jobId`/errors for batch `images` and `texts` sub-items to prevent retry resume interference; cascading duplication for Group nodes to include all children and internal connections; and auto-validating group containment on single-node duplication.
The Chinese version contains the complete item-by-item acceptance checklist.

